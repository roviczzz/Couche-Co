<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= typeof title !== 'undefined' ? title : 'Blessings Cafe' %></title>
  <% if (!user) { %>
    <link rel="stylesheet" href="/css/navbar.css">
  <% } %>
</head>
<body>
  <% if (!user) { %>
    <header>
      <nav>
        <div class="logo">
          <img src="/resources/logo.png" alt="Blessings Cafe Logo">
          <span>Blessings Cafe</span>
        </div>
        <div class="nav-links">
          <a href="/">Home</a>
          <a href="/menu">Menu</a>
          <a href="/about">About Us</a>
          <a href="/contact">Contact</a>
        </div>
        <div class="account-menu">
          <button class="account-btn" onclick="toggleAccountDropdown()">
            <svg viewBox="0 0 24 24" width="24" height="24" fill="currentColor">
              <path d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z"/>
            </svg>
          </button>
          <div id="account-dropdown" class="account-dropdown">
            <a href="/auth/login">Login</a>
            <a href="/auth/register" class="register-link">Register</a>
          </div>
        </div>
      </nav>
    </header>
  <% } %>

  <%- body %>

  <% if (!user) { %>
    <script src="/js/navbar.js"></script>
  <% } %>
</body>
</html>
