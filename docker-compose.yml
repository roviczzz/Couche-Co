version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - uploads_data:/app/public/uploads
    environment:
      - NODE_ENV=production
    restart: unless-stopped

volumes:
  uploads_data: